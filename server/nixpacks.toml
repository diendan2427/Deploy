# Nixpacks configuration for Railway
# Auto-detect and build Node.js TypeScript project

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
